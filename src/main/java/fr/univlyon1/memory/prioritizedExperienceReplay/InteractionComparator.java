package fr.univlyon1.memory.prioritizedExperienceReplay;

import java.util.Comparator;

public class InteractionComparator<InteractionHistory> implements Comparator<InteractionHistory> {

    @Override
    public int compare(InteractionHistory interactionHistory, InteractionHistory t1) {
        //return interactionHistory.
        return 0;
    }
}
